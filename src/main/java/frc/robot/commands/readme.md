# Hello!
This commands package is for commands that require 
motion with a non-constant speed. 

For example, a shooter
would be constant speed, so all you'd need to do is toggle it 
with a button, and the command could be declared in the subsystem
file itself. But for subsystems that need dynamic speed and
an axis controller, these types of commands are better.